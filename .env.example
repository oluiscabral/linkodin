# OpenAI API Configuration
OPENAI_API_KEY=your-openai-api-key-here

# Model Configuration (optional, defaults to gpt-5)
# Note: GPT-5 models don't support custom temperature parameters
# Use gpt-4 or gpt-4-turbo if you need temperature control
OPENAI_MODEL=gpt-5

# Alternative models for temperature support:
# OPENAI_MODEL=gpt-4
# OPENAI_MODEL=gpt-4-turbo
